{
  "name": "north-star-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end tests (Playwright + Page Object Model) for North Star.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  }
}
